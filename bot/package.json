{
  "name": "tridelphi-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker webhook front door for the TriDelPhi scan bot.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/verify.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
